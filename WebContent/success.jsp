<%-- Message set normally: ${message}
Message set through ModelAttribute: ${ownMsg}
Form data from ModelAttribute: ${student.name} --%>
${student.name}	registered successfully with email ${student.email}